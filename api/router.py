# FastAPI Router
# Definește endpoint-urile HTTP:
# - GET /weather/{location} - obține date meteo pentru o locație
# - GET /weather/history/{location} - istoric date
# - POST /weather/aggregate - agregare date de la furnizori
# Injectează dependențele (service, adapters) și gestionează request/response
