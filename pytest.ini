[pytest]
pythonpath = backend
testpaths =
    tests/unit
    tests/integration
    tests/service
    tests/pact/consumer
    tests/pact/provider
markers =
    integration: integration tests (requires Docker)
    service: service tests (requires Docker)
